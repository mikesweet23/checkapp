"use client";

import Link from "next/link";
import { useState } from "react";
import { AdminSidebar } from "@/src/components/admin-sidebar";
import type { Assessment, AssessmentDraft, Question, ResultBand, ScoreCategory } from "@/src/lib/types";

const makeId = (prefix: string) => `${prefix}-${crypto.randomUUID()}`;

function starterDraft(initial?: Assessment): AssessmentDraft {
  if (initial) return { id: initial.id, name: initial.name, shortName: initial.shortName, slug: initial.slug, tagline: initial.tagline, description: initial.description, completionMinutes: initial.completionMinutes, status: initial.status, categories: initial.categories, questions: initial.questions, resultBands: initial.resultBands };
  const category: ScoreCategory = { id: makeId("category"), name: "Overall wellbeing", description: "The overall pattern in your answers.", color: "#ef6d3f" };
  const question: Question = { id: makeId("question"), prompt: "How are things feeling for you at the moment?", helpText: "Choose the response that feels closest to your experience.", type: "SINGLE_CHOICE", categoryId: category.id, options: [{ id: makeId("option"), label: "Almost never", score: 0 }, { id: makeId("option"), label: "Occasionally", score: 1 }, { id: makeId("option"), label: "Often", score: 2 }, { id: makeId("option"), label: "Almost always", score: 3 }] };
  const makeBand = (minScore: number, maxScore: number, label: string, title: string): ResultBand => ({ id: makeId("band"), minScore, maxScore, label, title, summary: "Your answers suggest there is a useful pattern to notice.", body: "Use this space to explain the result in a warm, useful way.", ctaLabel: "Learn more", ctaHref: "https://absolutemind.co.uk/", videoTitle: "A calmer way forward", videoDescription: "Add the supporting result content here.", pdf: { heading: "PERSONALISED REPORT", introduction: "A thoughtful snapshot of the patterns behind your answers.", nextStep: "Add the next-step copy for this result.", note: "Add the closing note for this result." } });
  return { name: "Your new assessment", shortName: "New check-in", slug: "your-assessment", tagline: "A clear, thoughtful check-in.", description: "Tell people what this check-in will help them understand.", completionMinutes: 3, status: "DRAFT", categories: [category], questions: [question], resultBands: [makeBand(0, 24, "A steadier starting point", "You have a useful foundation of calm"), makeBand(25, 49, "A mind asking for space", "You may be carrying more than it looks"), makeBand(50, 74, "A pattern worth changing", "This may be affecting your everyday life"), makeBand(75, 100, "A clear invitation to support", "It may be time to stop facing this alone")] };
}

export function NewAssessmentFormV2({ initial }: { initial?: Assessment }) {
  const [draft, setDraft] = useState<AssessmentDraft>(() => starterDraft(initial));
  const [message, setMessage] = useState("");
  const [error, setError] = useState("");
  const [busy, setBusy] = useState(false);
  const [savedSlug, setSavedSlug] = useState(initial?.slug ?? "");
  const update = <K extends keyof AssessmentDraft>(key: K, value: AssessmentDraft[K]) => setDraft((current) => ({ ...current, [key]: value }));
  const updateQuestion = (id: string, patch: Partial<Question>) => setDraft((current) => ({ ...current, questions: current.questions.map((question) => question.id === id ? { ...question, ...patch } : question) }));
  const updateBand = (id: string, patch: Partial<ResultBand>) => setDraft((current) => ({ ...current, resultBands: current.resultBands.map((band) => band.id === id ? { ...band, ...patch } : band) }));
  const updateCategory = (id: string, patch: Partial<ScoreCategory>) => setDraft((current) => ({ ...current, categories: current.categories.map((category) => category.id === id ? { ...category, ...patch } : category) }));
  const save = async (status: "DRAFT" | "LIVE") => {
    setBusy(true); setError(""); setMessage("");
    const payload = { ...draft, status };
    if (!payload.name.trim() || !payload.slug.trim()) { setError("Add a name and public URL first."); setBusy(false); return; }
    try {
      const response = await fetch("/api/admin/assessments", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) });
      const body = await response.json();
      if (!response.ok) throw new Error(body.error ?? "We could not save this assessment.");
      setDraft((current) => ({ ...current, id: body.id, status })); setSavedSlug(body.slug); setMessage(status === "LIVE" ? "Published and ready to share." : "Draft saved."); window.history.replaceState(null, "", `/admin/assessments/${body.id}`);
    } catch (saveError) { setError(saveError instanceof Error ? saveError.message : "We could not save this assessment."); } finally { setBusy(false); }
  };
  const addQuestion = () => setDraft((current) => ({ ...current, questions: [...current.questions, { id: makeId("question"), prompt: "", helpText: "", type: "SINGLE_CHOICE", categoryId: current.categories[0]?.id, options: [{ id: makeId("option"), label: "", score: 0 }, { id: makeId("option"), label: "", score: 1 }] }] }));
  const removeQuestion = (id: string) => setDraft((current) => ({ ...current, questions: current.questions.filter((question) => question.id !== id) }));
  const moveQuestion = (id: string, targetIndex: number) => setDraft((current) => { const currentIndex = current.questions.findIndex((question) => question.id === id); if (currentIndex < 0 || currentIndex === targetIndex) return current; const questions = [...current.questions]; const [question] = questions.splice(currentIndex, 1); questions.splice(targetIndex, 0, question); return { ...current, questions }; });
  const addCategory = () => setDraft((current) => ({ ...current, categories: [...current.categories, { id: makeId("category"), name: "New category", description: "", color: "#ef6d3f" }] }));
  const addBand = () => setDraft((current) => ({ ...current, resultBands: [...current.resultBands, { id: makeId("band"), minScore: 0, maxScore: 100, label: "New result", title: "Your result", summary: "", body: "", ctaLabel: "Learn more", ctaHref: "https://absolutemind.co.uk/", videoTitle: "", videoDescription: "", pdf: { heading: "PERSONALISED REPORT", introduction: "", nextStep: "", note: "" } }] }));

  return <div className="admin-shell"><AdminSidebar active="Assessments" /><main className="admin-main"><div className="page-title"><div><div className="eyebrow">Assessment studio</div><h1>{initial ? "Edit assessment" : "Create an assessment"}</h1><p>Build the questions, scoring and result experience Paula wants to share.</p></div><Link className="button button-ghost" href="/admin/assessments">← Back to assessments</Link></div>{error && <p className="form-error" role="alert">{error}</p>}{message && <p className="success-message" role="status">{message} {savedSlug && draft.status === "LIVE" && <a href={`/assessments/${savedSlug}`} target="_blank" rel="noreferrer">Open public link ↗</a>}</p>}<div className="builder-stack">
    <section className="form-card"><h2>1. Basics</h2><p>These are the details people see before they begin.</p><div className="builder-grid"><div className="field"><label htmlFor="name">Assessment name</label><input id="name" value={draft.name} onChange={(event) => update("name", event.target.value)} /></div><div className="field"><label htmlFor="shortName">Short name</label><input id="shortName" value={draft.shortName} onChange={(event) => update("shortName", event.target.value)} /></div><div className="field"><label htmlFor="slug">Public URL ending</label><input id="slug" value={draft.slug} onChange={(event) => update("slug", event.target.value.toLowerCase().replace(/[^a-z0-9-]+/g, "-"))} /><small className="field-hint">check.absolutemind.co.uk/assessments/{draft.slug}</small></div><div className="field"><label htmlFor="minutes">Estimated minutes</label><input id="minutes" type="number" min="1" max="60" value={draft.completionMinutes} onChange={(event) => update("completionMinutes", Number(event.target.value))} /></div></div><div className="field"><label htmlFor="tagline">Short tagline</label><input id="tagline" value={draft.tagline} onChange={(event) => update("tagline", event.target.value)} /></div><div className="field"><label htmlFor="description">Introduction</label><textarea id="description" value={draft.description} onChange={(event) => update("description", event.target.value)} /></div></section>
    <section className="form-card"><div className="builder-heading"><div><h2>2. Categories</h2><p>Optional areas to show in the result breakdown and PDF.</p></div><button className="button button-ghost" type="button" onClick={addCategory}>+ Add category</button></div>{draft.categories.map((category) => <div className="builder-item" key={category.id}><div className="builder-grid"><div className="field"><label>Category name</label><input value={category.name} onChange={(event) => updateCategory(category.id, { name: event.target.value })} /></div><div className="field"><label>Colour</label><input value={category.color} onChange={(event) => updateCategory(category.id, { color: event.target.value })} /></div></div><div className="field"><label>Description</label><input value={category.description} onChange={(event) => updateCategory(category.id, { description: event.target.value })} /></div></div>)}</section>
    <section className="form-card"><div className="builder-heading"><div><h2>3. Questions and scoring</h2><p>Use single choice, rating scale or optional written answers. Scores are attached to selectable answers.</p></div><button className="button button-ghost" type="button" onClick={addQuestion}>+ Add question</button></div>{draft.questions.map((question, questionIndex) => <div className="builder-item" key={question.id}><div className="question-builder-heading"><div className="eyebrow">Question {questionIndex + 1}</div><div className="question-builder-actions"><label className="question-position">Position<select aria-label={`Position for question ${questionIndex + 1}`} value={questionIndex + 1} onChange={(event) => moveQuestion(question.id, Number(event.target.value) - 1)}>{draft.questions.map((_, position) => <option key={position} value={position + 1}>Question {position + 1}</option>)}</select></label><button className="button button-ghost button-danger" type="button" onClick={() => removeQuestion(question.id)}>Delete question</button></div></div><div className="builder-grid"><div className="field"><label>Question</label><input value={question.prompt} onChange={(event) => updateQuestion(question.id, { prompt: event.target.value })} /></div><div className="field"><label>Question type</label><select value={question.type ?? "SINGLE_CHOICE"} onChange={(event) => updateQuestion(question.id, { type: event.target.value as Question["type"] })}><option value="SINGLE_CHOICE">Single choice</option><option value="SCALE">Rating scale</option><option value="TEXT">Written answer</option></select></div></div><div className="field"><label>Help text</label><input value={question.helpText ?? ""} onChange={(event) => updateQuestion(question.id, { helpText: event.target.value })} /></div>{question.type === "TEXT" ? <label className="consent-row"><input type="checkbox" checked={Boolean(question.optional)} onChange={(event) => updateQuestion(question.id, { optional: event.target.checked })} /><span>Optional written answer</span></label> : <div className="option-editor">{question.options.map((option) => <div className="option-editor-row" key={option.id}><input value={option.label} placeholder="Answer label" onChange={(event) => updateQuestion(question.id, { options: question.options.map((candidate) => candidate.id === option.id ? { ...candidate, label: event.target.value } : candidate) })} /><input type="number" value={option.score} aria-label="Score" onChange={(event) => updateQuestion(question.id, { options: question.options.map((candidate) => candidate.id === option.id ? { ...candidate, score: Number(event.target.value) } : candidate) })} /><button className="button button-ghost" type="button" onClick={() => updateQuestion(question.id, { options: question.options.filter((candidate) => candidate.id !== option.id) })}>Remove</button></div>)}<button className="button button-ghost" type="button" onClick={() => updateQuestion(question.id, { options: [...question.options, { id: makeId("option"), label: "", score: question.options.length }] })}>+ Add answer</button></div>}</div>)}</section>
    <section className="form-card"><div className="builder-heading"><div><h2>4. Results and PDF content</h2><p>Each score band gets its own result page copy and PDF content, using the same branded layout.</p></div><button className="button button-ghost" type="button" onClick={addBand}>+ Add result band</button></div>{draft.resultBands.map((band, bandIndex) => <div className="builder-item" key={band.id}><div className="eyebrow">Result band {bandIndex + 1}</div><div className="builder-grid"><div className="field"><label>Minimum score</label><input type="number" value={band.minScore} onChange={(event) => updateBand(band.id, { minScore: Number(event.target.value) })} /></div><div className="field"><label>Maximum score</label><input type="number" value={band.maxScore} onChange={(event) => updateBand(band.id, { maxScore: Number(event.target.value) })} /></div><div className="field"><label>Band label</label><input value={band.label} onChange={(event) => updateBand(band.id, { label: event.target.value })} /></div><div className="field"><label>Result title</label><input value={band.title} onChange={(event) => updateBand(band.id, { title: event.target.value })} /></div></div><div className="field"><label>Summary shown at the top of the result</label><textarea value={band.summary} onChange={(event) => updateBand(band.id, { summary: event.target.value })} /></div><div className="field"><label>Result page detail</label><textarea value={band.body} onChange={(event) => updateBand(band.id, { body: event.target.value })} /></div><div className="builder-grid"><div className="field"><label>Button label</label><input value={band.ctaLabel} onChange={(event) => updateBand(band.id, { ctaLabel: event.target.value })} /></div><div className="field"><label>Button link</label><input value={band.ctaHref} onChange={(event) => updateBand(band.id, { ctaHref: event.target.value })} /></div></div><div className="builder-subheading">PDF content for this score band</div><div className="builder-grid"><div className="field"><label>PDF heading</label><input value={band.pdf?.heading ?? ""} onChange={(event) => updateBand(band.id, { pdf: { ...band.pdf, heading: event.target.value } })} /></div><div className="field"><label>PDF introduction</label><textarea value={band.pdf?.introduction ?? ""} onChange={(event) => updateBand(band.id, { pdf: { ...band.pdf, introduction: event.target.value } })} /></div><div className="field"><label>PDF next step</label><textarea value={band.pdf?.nextStep ?? ""} onChange={(event) => updateBand(band.id, { pdf: { ...band.pdf, nextStep: event.target.value } })} /></div><div className="field"><label>PDF closing note</label><textarea value={band.pdf?.note ?? ""} onChange={(event) => updateBand(band.id, { pdf: { ...band.pdf, note: event.target.value } })} /></div></div></div>)}</section>
  </div><div className="builder-actions"><button className="button button-ghost" type="button" disabled={busy} onClick={() => save("DRAFT")}>Save draft</button><button className="button button-primary" type="button" disabled={busy} onClick={() => save("LIVE")}>{busy ? "Saving…" : "Publish check-in →"}</button></div></main></div>;
}
