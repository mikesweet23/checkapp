import Link from "next/link";
import { AdminSidebar } from "@/src/components/admin-sidebar";
import { assessments } from "@/src/lib/seed-data";

export default function AdminDashboard() {
  const live = assessments.filter((a) => a.status === "LIVE");
  return <div className="admin-shell"><AdminSidebar active="Overview" /><main className="admin-main"><div className="admin-top"><div><div className="eyebrow">Wednesday, 18 September 2026</div><h1>Good morning, Mike & Paula</h1><p>Here is how your scorecards are performing this week.</p></div><div className="admin-actions"><Link className="button button-ghost" href="/">View public site ↗</Link><Link className="button button-primary" href="/admin/assessments/new">+ New assessment</Link></div></div><div className="stat-grid"><div className="stat-card"><p>Active assessments</p><strong>{live.length}</strong><span className="stat-change">↑ 1 this month</span></div><div className="stat-card"><p>Total completions</p><strong>186</strong><span className="stat-change">↑ 18%</span></div><div className="stat-card"><p>Average completion</p><strong>3m 12s</strong><span className="stat-change">↑ 8%</span></div><div className="stat-card"><p>Result → enquiry</p><strong>42%</strong><span className="stat-change">↑ 6%</span></div></div><div className="dashboard-grid"><section className="panel"><div className="panel-heading"><h2>Your scorecards</h2><Link href="/admin/assessments">View all →</Link></div>{assessments.map((assessment) => <div className="assessment-row" key={assessment.id}><div className="row-main"><div className="row-avatar">✦</div><div><strong>{assessment.name}</strong><span>{assessment.questionCount || "No"} questions · Updated {assessment.updatedAt}</span></div></div><span className={`tag ${assessment.status === "DRAFT" ? "draft" : ""}`}>{assessment.status}</span></div>)}</section><section className="panel"><div className="panel-heading"><h2>Recent activity</h2><span className="muted" style={{ fontSize: 11 }}>This week</span></div><div className="activity-row"><div className="activity-dot" /><div><p><strong>New result</strong> — Anxiety Check</p><span>Alexandra M. · 14 minutes ago</span></div></div><div className="activity-row"><div className="activity-dot" /><div><p><strong>Assessment shared</strong> — Anxiety Check</p><span>via your public link · 2 hours ago</span></div></div><div className="activity-row"><div className="activity-dot" /><div><p><strong>New contact</strong> — Jamie R.</p><span>Requested a conversation · yesterday</span></div></div></section></div></main></div>;
}
